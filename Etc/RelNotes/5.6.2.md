# zsh 5.6.2 release notes

## Security fixes

n/a

## New features

n/a

## Incompatibilities

n/a

## Other

- Fixed another `SIGTTOU` case. (See 5.6.1 notes.)

- Fixed `SIGWINCH` being ignored when zsh is not in the foreground.

- The release tarballs are now compressed using `xz(1)`, whereas previously both
  `xz(1)` and `gzip(1)` versions were available.  If this gets in your way, give
  us a shout.
